# AGENTS.md

## Critical Rules (Do Not Break)

- Never manually edit generated files.
- If a file is generated, change the source and regenerate.
- Before editing any file, check if it has a generator comment/header.

## Generated Files In This Repo

- src/core/backend/bindings.ts
  - Generated by Tauri Specta.
  - Source of truth: Rust types/commands in src-tauri/src/**.
  - Regenerate with: cargo test export_bindings -- --nocapture (from src-tauri).

- src-tauri/schemas/plugin-manifest.schema.rs
  - Generated from JSON schema.
  - Source of truth: src-tauri/schemas/plugin-manifest.schema.json.
  - Do not hand-edit .schema.rs.

## How To Implement Backend API Changes Safely

1. Update Rust domain types/commands in src-tauri/src/**.
2. Build/check Rust first:
   - cd src-tauri && cargo check
3. Regenerate TS bindings from Rust (never patch bindings.ts by hand):
   - cd src-tauri && cargo test export_bindings -- --nocapture
4. Build frontend:
   - cd .. && npm run build

## SQL Safety Rules

- In JOIN queries, always qualify column names with table alias (e.g. m.type_json).
- Avoid SELECT with ambiguous names when tables share columns.
- Prefer explicit column lists over wildcard selections.

## Plugin Metrics Feature Notes

- Metrics are runtime metadata, not plugin endpoint outputs.
- Runtime API available for plugins:
  - openrisk.metrics.set(name, value)
  - openrisk.metrics.get(name)
  - openrisk.metrics.inc(name, delta)
  - openrisk.metrics.all()
- Metric declarations are read from plugin.json field metrics[].
- Metric value must match declared type; invalid values are dropped with warn log.

## Workflow Guardrail

- If a generated file was changed manually by mistake:
  1. Revert only that generated file change.
  2. Regenerate it from source.
  3. Re-run checks.
