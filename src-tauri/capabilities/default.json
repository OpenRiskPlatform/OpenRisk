{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "shell:allow-open",
    "core:default",
    "dialog:default",
    "mcp-bridge:default",
    "store:allow-load",
    "store:allow-set",
    "store:default"
  ]
}
